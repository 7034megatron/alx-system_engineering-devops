# First, ensure that ufw is installed and enabled
sudo apt-get update
sudo apt-get install ufw
sudo ufw enable

# Now, add a forwarding rule for port 8080 to port 80
sudo ufw route allow proto tcp from any to any port 8080

# Check the status of ufw to verify the changes
sudo ufw status

# Save the ufw configuration
sudo ufw reload
